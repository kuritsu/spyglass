{{page-title "Home"}}
<TreeViewFilters @display={{this.display}} @onDisplayChange={{this.onDisplayChange}} />
{{#each @model as |item|}}
<TreeView @target={{item}} @display={{this.display}} />
{{/each}}
